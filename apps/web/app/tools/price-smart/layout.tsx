// =====================================================================
// Layout cho /tools/price-smart — không cần logic riêng
// =====================================================================

export default function PriceSmartLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}